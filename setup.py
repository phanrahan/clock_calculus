from setuptools import setup
import sys

setup(
    name='clock',
    version='0.1-alpha',
    description='Implementation of the clock calculus using 2-adic numbers',
    scripts=[],
    packages=[
        "clock",
    ],
    python_requires='>=3.6'
)
